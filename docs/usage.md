# Usage

```bash
pip install -e ".[dev]"
dimfort check path/to/project
```

CLI commands:
- `dimfort check <paths>` — check Fortran source files for unit homogeneity.
- `dimfort lsp` — start the language server over stdio.
